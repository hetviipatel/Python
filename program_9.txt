Q1
1.string has no attribute reverse
2.error is occure due to name is string and string hasn't any method named reverse
3. name = "Radar"
   if name == name[::-1]:
     print("Palindrome)

Q2
1.out of index
2.s can contain 5 charater but index is starting from 0 so it occur
3. s="Hello"
   print(s[4])

Q3
1.string is immutable
2.string are immutable that's why it not assign any value
3. s=""
   s="J"

Q4
Q5

Q6
1.out of index
2.num has 3 element and range is end at 4 
3. nums = [1,2,3]
   for i in range(len(nums)):
       print(nums[i])
Q7
1.sytax error
2.number.append has square braket insted of round braket
3.numbers = [1,2,3]
  numbers.append(4)

Q8
1.out of index
2.pop contain 3rd index but list's indexing start with 0 
3.nums = [1,2,3]
  print(nums.pop(3))

Q9

Q10
1.out of index
2.lst doesn't containing any values and next line 10 at index 0 
3.lst = []
  lst.append(10)

Q11
1.tuple is immutable
2.tuple are immutable so once it's created it doesn't change it's value
3. t= (1,2,3)
   
Q12
1.incorrect syntax
2.list can be declared in square braket
3.s=[1,2,3]
  s.append(4)

Q13
1.out of index
2.set is null means it's doesn't contain any value 
3.


Q14
1.undefined name
2.printing c's value but it doesn't exists
3. d= {"a":1 , "b":2 , "c":3}
   print(d["c"])

Q15
1.syntax error
2.for loop is printing key and if we want all item to access than use emp.item()
3.emp = {"name": "John", "age": 30}
   for k, v in emp.items():
    print(k, v)

Q16

Q17
1.syntax error
2.if has syntax like if than condition and than semicolon
3.n=10
  if n>5:
     print("Grater")

Q18
1.syntax error
2.in if's condition there only one equal insted on ==
3.n=7
  if n %2 == 0:
     print("Even")

Q19
1.not error but printing incorrect way
2.it's has if condition means it's check first if and than also p check second if so insted of if it can be elif
3.x=5
  if x>10:
    print("A")
  elif x>3:
    print("B")
  else:
    print("c")

Q20
1.logical error
2.in while loop it's doesn't increase i means it's always to 1 and loop will be infinate
3.i=1
  while i<=5:
     print(i)
     i = i+1

Q21
1.indentation error
2.after completing for loop's line the printing statement should be in proper indentation
3.for i in range(5):
    print(i)

Q22

Q23
1.logical error
2.no output bcz it's always i=10 means it's value is doesn't change
3. i =10
   while i<5:
     print(i)
     i=i-1
Q24
1.logical error
2.for loops is always increase so it's not required
3.for i in range(1, 5):
    print(i)

Q25
1.str can not check insted
2.age is taking from user and that's bydefault it take as string so it can't be change
3. age = int(input())
   if age > 18:
      print("Eligible")

Q26
1.string concenition
2.x and y are strings so it's can be concet but not addition 
3. x=5 
   x=3
   print(x+y)

Q27
1.there is some missing
2. in if statement there is no condition so it's syntax error
3. salary = -5000
   if salary<0:
       print("valid")

Q28
1.logical error
2. 1 is always be prime so it has one another condition and loop ends at n so it's end with 1 and start with 2 that is in logical
3.n = 1
if n<=1:
   print("prime")
for i in range(2, n):
    if n % i == 0:
        print("Not Prime")
        break
else:
    print("Prime")

Q29
1.zerodivsible
2.nums list is empty and at the end it's divided by it's length
3.nums = []
total = 0
for n in nums:
    total += n

Q30
1.logical error
2.bydefault split take space and we want split with comma
3.skills = ",Java,C"
skills = skills.split(",")
print(len(skills))